import {Util} from './util'

function createSlideArtist(typeTime) {
    const queryResults = Util.topArtist(typeTime, 5)

    artistDiv = document.querySelector('#topArtist')
    let h2 = document.createElement('h2')
    h2.dataset
}

function createSlideTrack(typeTime) {

}